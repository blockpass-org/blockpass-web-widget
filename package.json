{
  "name": "blockpass-public",
  "scripts": {
    "sdk:test": "bun run --cwd packages/web-widget test",
    "sdk:build": "bun run --cwd packages/web-widget build",
    "sdk:pack": "bun run sdk:build && bun pm --cwd packages/web-widget pack",
    "example:react:dev": "bun run --cwd examples/react-client-site dev",
    "example:kycc_api:dev": "bun run --cwd examples/kycc-api-first dev"
  },
  "workspaces": [
    "./packages/*",
    "./examples/*"
  ]
}